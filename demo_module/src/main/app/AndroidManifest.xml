<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cgw.demo_module">

    <application android:name=".base.MyApplication">

        <activity
            android:name=".ui.activity.DemoActivity1"
            android:configChanges="screenSize|orientation|keyboardHidden|screenLayout"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.MainActivity"
            android:configChanges="screenSize|orientation|keyboardHidden|screenLayout"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.activity.TimeAxisActivity"
            android:configChanges="screenSize|orientation|keyboardHidden|screenLayout"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.activity.BMIMainActivity"
            android:configChanges="screenSize|orientation|keyboardHidden|screenLayout"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:screenOrientation="portrait" />

        <meta-data
            android:name="design_width_in_dp"
            android:value="360" />
        <meta-data
            android:name="design_height_in_dp"
            android:value="640" />

    </application>


</manifest>
